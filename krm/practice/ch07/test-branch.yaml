apiVersion: repo.github.upbound.io/v1alpha1
kind: Branch
metadata:
  name: crossplane-test-branch
spec:
  forProvider:
    repository: learning-crossplane  # just the repo name, not the full URL
    branch: feature/crossplane-test
    sourceBranch: main
  providerConfigRef:
    name: default

