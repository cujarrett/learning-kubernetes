apiVersion: platform.example.io/v1alpha1
kind: WebService
metadata:
  name: pinned-svc
  namespace: default
spec:
  crossplane:
    compositionUpdatePolicy: Manual
  image: nginx:alpine
  replicas: 1
  port: 80
  environment: production
